<<%= node.tag %> 
  <% for (k, v) in node.attributes.as_object().unwrap() { %>
    <%= k %> = "<%= v.as_str().unwrap() %>"
  <% } %>
>
  <% if let Some(text) = &node.text { %>
    <%= text %>
  <% } %>
  <%- render_children_nodes(node, nodes) %>
</<%= node.tag %>>
